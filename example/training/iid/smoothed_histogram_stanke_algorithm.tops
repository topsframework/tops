// -*- mode: c++ -*-
// vim: ft=chaiscript:

model_type = "IID"

observations = [ "1", "2", "3", "4", "5", "6" ]

training_algorithm = "SmoothedHistogramStanke"

training_set = dataset("data/cassino-dice.tsv")

weights = weights("data/cassino-dice.weights")

maximum_length = 123

m = 123

slope = 1.23
