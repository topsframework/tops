// -*- mode: c++ -*-
// vim: ft=chaiscript:

model_type = "PeriodicIMC"

observations = [ "0", "1" ]

training_algorithm = "Interpolation"

training_set = dataset("data/cassino-coin.tsv")

initial_model = pretrained_model("periodic_imc.tops")

weights = dataset("data/cassino-coin-weights.tsv")

order = 3

number_of_phases = 3

pseudo_counter = 1.23
