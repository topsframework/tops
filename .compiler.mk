
########################################################################
##                    ToPS Compiler Configuration                     ##
########################################################################

AR     := $(or $(shell echo $$AR),$(AR))
AS     := $(or $(shell echo $$AS),$(AS))
CC     := $(or $(shell echo $$CC),$(CC))
FC     := $(or $(shell echo $$FC),$(FC))
CXX    := $(or $(shell echo $$CXX),$(CXX))
RANLIB := $(or $(shell echo $$RANLIB),$(RANLIB))
